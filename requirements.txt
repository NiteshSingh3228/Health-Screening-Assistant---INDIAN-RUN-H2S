# ══════════════════════════════════════════════════════════════════════════════
#  Health Screening Assistant — Unified Requirements
#  Combines:  requirements (2).txt  +  requirements.txt
#
#  Install:   pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════════════

# ── Web API ───────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0

# ── ML / Data Science ─────────────────────────────────────────────────────────
scikit-learn>=1.4.0          # Random Forest, PCA, metrics
scikit-image>=0.20.0         # HOG feature extraction
sentence-transformers>=2.2.2 # Vector embeddings for semantic search
joblib>=1.3.0                # model serialization
numpy>=1.26.0
pandas>=2.2.0
Pillow>=10.2.0               # image loading & resizing

# ── Deep Learning (optional — for Keras CNN image models) ────────────────────
# tensorflow>=2.16.0         # uncomment if you want to use train_image_model.py

# ── AI / LLM Integrations (optional — install one or both) ───────────────────
openai>=1.30.0               # GPT-4o-mini summaries & chat
anthropic>=0.28.0            # Claude fallback

# ── Notebook / Analysis (optional — for the .ipynb notebooks) ─────────────────
# jupyter>=1.0.0
# matplotlib>=3.8.0
# seaborn>=0.13.0

# ── Dashboard (optional — legacy streamlit app from requirements (2).txt) ─────
# streamlit>=1.33.0
